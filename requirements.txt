# Whisper ASR API — Production Dependencies
# Install: pip install -r requirements.txt
#
# Versions are pinned for reproducible deployments (B-14).
# For hash-locked production deploys, run:
#   pip-compile --generate-hashes requirements.in -o requirements.txt

# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Whisper
faster-whisper==1.1.1

# Configuration
pydantic-settings==2.7.1

# Logging
structlog==24.4.0

# Rate limiting
slowapi==0.1.9

# Audio processing
numpy==2.2.3

# HTTP file upload
python-multipart==0.0.20

# Async file I/O (non-blocking upload streaming)
aiofiles==23.2.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0

# HTTP client (for callbacks to main backend)
httpx==0.28.1
